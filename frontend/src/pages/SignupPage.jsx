import { useState } from 'react'

export default function SignupPage({ onBack, onSuccess, onSignIn }) {
  const [username, setUsername] = useState(''), [email, setEmail] = useState(''), [password, setPassword] = useState(''), [confirm, setConfirm] = useState(''), [show, setShow] = useState(false), [notice, setNotice] = useState(''), [busy, setBusy] = useState(false)
  const submit = async event => { event.preventDefault(); if (!username.trim() || !email.trim() || !password) return setNotice('Please complete every field.'); if (password !== confirm) return setNotice('Passwords do not match.'); setBusy(true); setNotice(''); try { const response = await fetch('/api/auth/signup', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ username: username.trim(), email: email.trim(), password }) }); const data = await response.json(); if (!response.ok) throw Error(data.detail || data.message || 'Could not create your account.'); setNotice('Account created. Please sign in.'); setTimeout(() => onSuccess?.(), 600) } catch (error) { setNotice(error.message || 'Could not create your account.') } finally { setBusy(false) } }
  return <div className="auth-page"><button className="back-link" onClick={onBack}>← Back</button><main className="auth-card"><p className="eyebrow">JOIN S-SPARSH</p><h1>Create your account</h1><p>Start your Indian Sign Language learning journey.</p><form onSubmit={submit}><label>Username<input value={username} onChange={e => setUsername(e.target.value)} autoComplete="username"/></label><label>Email<input type="email" value={email} onChange={e => setEmail(e.target.value)} autoComplete="email"/></label><label>Password<div className="password-field"><input type={show ? 'text' : 'password'} value={password} onChange={e => setPassword(e.target.value)} autoComplete="new-password"/><button type="button" onClick={() => setShow(v => !v)}>{show ? 'Hide' : 'Show'}</button></div></label><label>Confirm password<input type={show ? 'text' : 'password'} value={confirm} onChange={e => setConfirm(e.target.value)} autoComplete="new-password"/></label>{notice && <p className="auth-notice" role="status">{notice}</p>}<button className="btn-primary" disabled={busy}>{busy ? 'Creating account…' : 'Create account'}</button></form><button className="btn-ghost" onClick={onSignIn}>Already have an account? Sign in</button></main></div>
}
