export function CameraView({ recognition, mode }) {
  const { videoRef, running, prediction, confidence, hold, startCamera, stopCamera, switchCamera, canSwitchCamera } = recognition
  return <section className="camera-stage"><div className={`camera-container ${mode === 'conversation' ? 'compact' : ''}`}><video ref={videoRef} className={running ? '' : 'camera-video-hidden'} muted playsInline />{!running ? <div className="camera-placeholder"><div className="placeholder-ambient"/><div className="placeholder-content"><span className="camera-icon">◉</span><h3>{mode === 'learning' ? 'Ready to Sign' : 'Ready to Communicate'}</h3><p>{mode === 'learning' ? 'Position your hands inside the frame and begin signing.' : 'Start the camera to begin your ISL conversation.'}</p><button className="btn-primary" onClick={() => startCamera()}>▶ Start Camera</button></div></div> : <><div className="camera-overlay"><span className="live-indicator"><i/>Live</span><div>{canSwitchCamera && <button className="camera-flip" aria-label="Switch camera" onClick={switchCamera}>↻</button>}<button className="btn-stop" aria-label="Stop camera" onClick={stopCamera}>■</button></div></div><div className="scan-beam"/><div className="recognition-panel" aria-live="polite"><div className="recognition-display"><span className="recognition-letter">{prediction}</span><div className="recognition-label">{prediction === '·' ? 'No hand' : 'Detected'}</div></div><div className="recognition-metrics"><div className="metric-row"><span>Confidence</span><b>{confidence}%</b></div><div className="progress-bar"><i style={{width:`${confidence}%`}}/></div><div className="metric-row"><span>Hold to confirm</span><b>{hold}%</b></div><div className="progress-bar hold"><i style={{width:`${hold}%`}}/></div></div></div></>}<div className="camera-corners"><i className="corner tl"/><i className="corner tr"/><i className="corner bl"/><i className="corner br"/></div></div></section>
}
