import { useNavigate } from 'react-router-dom'
const Mark = () => <svg viewBox="0 0 32 32" width="28" height="28" fill="none" aria-hidden="true"><circle cx="16" cy="16" r="14" stroke="currentColor" opacity=".35"/><path d="M9.5 21c1.8-6.2 3.8-9.3 6.5-9.3s4.7 3.1 6.5 9.3" stroke="currentColor" strokeWidth="2.8" strokeLinecap="round"/><circle cx="16" cy="9.4" r="2.4" fill="currentColor"/></svg>
export function RecognitionHeader({ mode, running, health, dark, toggleTheme }) { const navigate = useNavigate(); return <header className="recognition-header"><div className="brand"><span className="brand-mark"><Mark/></span><div className="brand-text"><h1 className="brand-wordmark">S-स्पर्श</h1><p className="brand-tagline">Indian Sign Language <span className="sep">·</span> Sign → Speech</p></div></div><div className="mode-switch" role="tablist" aria-label="Recognition mode"><button className={mode === 'learning' ? 'active' : ''} onClick={() => navigate('/learning')}>Learning</button><button className={mode === 'conversation' ? 'active' : ''} onClick={() => navigate('/conversation')}>Conversation</button><span className="mode-thumb" style={{ transform: mode === 'learning' ? 'translateX(0%)' : 'translateX(100%)' }}/></div><div className="header-controls"><div className={`status-pill ${running ? 'live' : health?.modelReady ? 'ready' : 'warn'}`}><span className="status-dot"/><span className="status-label">{running ? 'Live' : health?.modelReady ? 'Ready' : 'Offline'}</span></div><button className="theme-toggle" aria-label="Toggle theme" onClick={toggleTheme}>{dark ? '☀' : '☾'}</button></div></header> }
